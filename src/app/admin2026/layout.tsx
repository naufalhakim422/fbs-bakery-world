'use client';

import AdminLayout from '@/app/admin/layout';
import React from 'react';

export default function Admin2026Layout({ children }: { children: React.ReactNode }) {
  return <AdminLayout>{children}</AdminLayout>;
}
